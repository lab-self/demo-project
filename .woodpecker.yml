steps:
  test:
    image: alpine:3.22
    commands:
      - echo "========================================"
      - echo "WOODPECKER CI TEST"
      - echo "========================================"
      - echo "Repository: ${CI_REPO}"
      - echo "Branch: ${CI_COMMIT_BRANCH}"
      - echo "Commit: ${CI_COMMIT_SHA}"
      - echo "Agent Docker backend is working"
      - echo "CI pipeline completed successfully"
      - echo "========================================"